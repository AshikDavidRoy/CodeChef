import java.util.Scanner;

public class FeverDetection {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        int T = scanner.nextInt(); // Number of test cases

        for (int i = 0; i < T; i++) {
            int X = scanner.nextInt(); // Body temperature of Chef

            if (X > 98) {
                System.out.println("YES"); // Chef has fever
            } else {
                System.out.println("NO"); // Chef does not have fever
            }
        }

        scanner.close();
    }
}
